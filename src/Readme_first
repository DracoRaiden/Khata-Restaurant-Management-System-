This contains all the code files to run this. We have used sqlalchemy api to connect to the backend. The code files are not organised so it might take you some time to understand how the system works. The backend is implemented in postgres sql pgadmin4. You need to connect that to you python code ny replacing the link. Take guidance from chatgpt how to combine all these and work them out.
